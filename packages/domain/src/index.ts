/**
 * Core business entities and domain logic land here as they're built —
 * framework-agnostic, no Next.js/React/ORM imports in this package.
 *
 * Deliberately empty in Phase 1 (architecture setup). The first real content
 * arrives with the shared property model (CLAUDE.md 27, priority 4), which
 * comes after the public website and authentication phases per the agreed
 * build order.
 */
export {};
